# BMW iDrive Launcher

[English](README.md) · **Русский**

**Кастомный Android-лаунчер (домашний экран), превращающий дешёвую китайскую магнитолу в премиальный интерфейс в стиле BMW iDrive.**

Сделан для **BMW X5 (E53) 2005 года** с головным устройством **XTRONS** на Android. Заменяет тормозной заводской рабочий стол быстрым тач-интерфейсом в духе современного **iDrive**: 3D-карусель плиток поверх **живой карты-фона**, янтарь приборной подсветки на почти-чёрном графите, экран «сейчас играет» для **Яндекс Музыки** и **написанный с нуля борткомпьютер, который читает BMW I-Bus машины напрямую по USB** — плюс обновления «по воздуху» (OTA) и перезагрузка в один тап. Всё на **Kotlin + Jetpack Compose**, покрыто скриншот-тестами, **без root**.

<p align="center">
  <img src="screenshots/home.png" alt="Главный экран — карусель" width="88%">
</p>

---

## Содержание

- [Зачем](#зачем)
- [Скриншоты](#скриншоты)
- [Возможности](#возможности)
- [Машина и голова](#машина-и-голова)
- [Дизайн-система](#дизайн-система)
- [Архитектура](#архитектура)
- [Структура проекта](#структура-проекта)
- [Сборка и запуск](#сборка-и-запуск)
- [Тесты](#тесты)
- [Обновления по воздуху (OTA)](#обновления-по-воздуху-ota)
- [История версий](#история-версий)
- [Планы](#планы)
- [Известные ограничения](#известные-ограничения)
- [Стек](#стек)
- [Авторы и лицензия](#авторы-и-лицензия)

---

## Зачем

Заводской лаунчер на этих XTRONS-головах (заброшенный сторонний «iDrive Launcher») тормозит и не поддерживается.

Всё началось как написанная с нуля замена, чья главная задача была — **автозапускать сторонний проприетарный i-Bus-апп** (борткомпьютер машины) и не мешать. С тех пор это выросло в полноценный стек: теперь лаунчер **сам читает I-Bus машины** через USB-адаптер Resler — так что тот сторонний апп **полностью убран** — и рисует быстрый, читаемый с одного взгляда домашний экран, которым удобно пользоваться на ходу с расстояния 70–90 см.

**Цели дизайна**

- **Быстро и отзывчиво** — без лагов на слабом Qualcomm.
- **Тач-центрично** — у E53 нет шайбы-контроллера, только тачскрин XTRONS.
- **Язык поверхностей BMW** — янтарь приборки на графите, объём в духе iDrive.
- **Владеть всем стеком** — борткомпьютер, обновления, перезагрузка — чтобы ничто не зависело от заброшенного стороннего аппа, и всё можно было улучшать по воздуху без флешки.

---

## Скриншоты

> Это реальный UI приложения, снятый скриншот-тестами [Paparazzi](https://github.com/cashapp/paparazzi) в родном разрешении головы 1280×720.

| Главный — 3D-карусель iDrive (поверх живой карты) | Музыка — плеер v4 (обложка-фон + спокойная полоса перемотки) |
|---|---|
| ![Главный](screenshots/home.png) | ![Музыка плеер v4](screenshots/music-player-v4.png) |

| Приложения | Настройки |
|---|---|
| ![Приложения](screenshots/apps.png) | ![Настройки](screenshots/settings.png) |

---

## Возможности

### Главный экран — крутящаяся 3D-карусель плиток
- **Бесконечно зацикленная цилиндрическая 3D-карусель** крупных плиток (в стиле iDrive-2016): центральная плитка плоская, в фокусе, с янтарной рамкой и свечением; боковые уходят в перспективу, уменьшаются и тускнеют.
- **Иконка-доминанта** на заполненных графитовых плитках; тап по всей плитке.
- Весь 3D-«характер» задаёт чистый, покрытый юнит-тестами `CarouselGeometry` (поворот / масштаб / прозрачность по позиции) — его можно тюнить и раскатывать по воздуху без правки Compose.
- **Статус-лента** (часы · полная русская дата · **живая наружная температура, прочитанная из I-Bus машины**) и «дышащее» **янтарное свечение**.
- **Полоски-индикатор**, масштабирующиеся под любое число плиток.
- Карусель парит поверх **живой карты-фона** (см. ниже).

### Живая карта-фон
- Домашний экран лежит поверх **живой карты, которая следует за GPS машины** — тёмная земля, тёмно-бирюзовая вода, янтарные дороги, без подписей: стилизована под лаунчер, а не сток-карта.
- Рисуется **MapLibre GL** из бесплатных векторных тайлов **OpenFreeMap** — **без API-ключа, без Google Play Services**. **Стиль — это JSON-файл на нашем собственном сервере**, поэтому цвета перенастраиваются без выпуска новой сборки.
- Тайлы **проксируются через наш собственный хост** (провайдер головы не достаёт до CDN тайлов напрямую, но до нашего сервера — всегда), а тёмный скрим держит тайлы/часы читаемыми поверх карты. Жесты отключены — это фон, а не карта, которую трогают. *(Ранняя попытка использовала Yandex MapKit, но его проприетарный формат стиля так и не применился — отказались.)*

### Музыка — «сейчас играет» из Яндекс Музыки (экран плеера v4)
- Управляет **Яндекс Музыкой** через штатный Android `MediaSession` / `MediaController` (без реверс-инжиниринга), используя `NotificationListenerService`.
- **Экран плеера v4:** **обложка альбома на весь фон** (скрим + виньетка для читаемости); **спокойная янтарная полоса перемотки** показывает прогресс. Ряд управления — *перемешать · пред · плей/пауза · след · лайк*, перемотка тапом и перетаскиванием. *(Ранний анимированный «эквалайзер» убрали: ROM головы блокирует захват аудио, так что любой анализатор был неизбежно синтетическим и читался как фейк.)*
- **Надёжный холодный старт:** открытие Музыки само будит Яндекс; если убитое приложение не заводится в фоне — явная кнопка **«Включить музыку»** запускает его на переднем плане, стартует «Моя волна», и возвращает на экран «сейчас играет».

### YouTube — за per-app VPN
- Плитка **YouTube**, открывающая приложение. YouTube в РФ режется, поэтому идёт через **VPN с обходом DPI** (VLESS + Reality на [sing-box](https://sing-box.sagernet.org/)) в режиме **постоянного _per-app_ `VpnService`** — через туннель уходит только трафик YouTube, всё остальное (Яндекс, навигация) напрямую. Без root в рантайме. Дизайн: [`youtube-vpn-design.md`](docs/superpowers/specs/2026-07-06-youtube-vpn-design.md).

### Борткомпьютер — читает I-Bus машины напрямую
- **Написанный с нуля борткомпьютер**: лаунчер общается с **BMW I-Bus** машины через USB-serial адаптер Resler **CP210x** (9600 8E1, через `usb-serial-for-android`) и сам декодирует broadcast-сообщения приборной панели IKE — **скорость, обороты, температуру ОЖ и наружную, зажигание** — и показывает их на нативном экране в стиле самого лаунчера.
- Разбор кадров I-Bus + декод — это **чистый, покрытый юнит-тестами** `IBusDecoder`; единственный **ридер на весь процесс** питает и экран борткомпьютера, и **живую наружную температуру** в статус-ленте главного экрана (борткомпьютер открывается уже подключённым).
- Это **заменило** старый подход с автозапуском проприетарного i-Bus-аппа, который был капризным и дрался за единственный USB-порт. Тот апп **удалён** — адаптером владеем мы. Ридер логирует по одному примеру каждого различимого типа сообщения на шине, чтобы новые датчики (топливо, расход, средние по поездке) можно было декодировать из реальной поездки.
- Всё, что касается USB, **защищено** — нет адаптера / нет разрешения просто показывает состояние «нет адаптера»; HOME-приложение не имеет права падать.

### Ящик приложений
- Сетка установленных приложений (реальные иконки из `PackageManager`) плюс отдельная, вынесенная в угол плитка **Перезагрузка** (тап-подтверждение).

### Настройки
- Сегмент-контрол темы **День / Ночь / Авто**, строка **приложений по умолчанию**, действие **«Сделать лаунчером по умолчанию»**, **встроенный апдейтер** и **загрузка диагностического лога** в один тап.

### Перезагрузка без root
- Перезагружает эту прошивку Microntek/HCT вендорным бродкастом (`com.microntek.hctreboot`) — подтверждённо работает из непривилегированного приложения, без `su`.

### Самообновление по воздуху
- Проверяет JSON-манифест и ставит новые сборки либо тихо (если вдруг есть root), либо через системный установщик (без root) — см. [OTA](#обновления-по-воздуху-ota).

---

## Машина и голова

| | |
|---|---|
| **Машина** | BMW X5 **E53** (2005) |
| **Голова** | XTRONS IQ7439BL |
| **Прошивка** | Microntek / HCT, **Android 13** |
| **SoC** | Qualcomm QCM6125 |
| **Root** | **Нет** (всё приложение спроектировано без root) |
| **Экран** | 1280×720 px @ 240 dpi → **853×480 dp**, ландшафт, ёмкостный тач (без энкодера) |
| **Интеграция с машиной** | свой I-Bus ридер через адаптер Resler **CP210x USB→I-Bus** |

> **Про дизайн:** всё свёрстано в **dp** под реальный экран 853 dp шириной. Скриншот-тесты намеренно рендерят в 1280 px / mdpi (чтобы `dp == px`) — так вёрстка привязана к макетам.

---

## Как включить ADB на таких головах — пароль `adbon`

Главная засада при разработке под такие головы: обычный трюк *«тапни Номер сборки 7 раз»* **не работает**, а пункт *«Для разработчиков»* не появляется — экран «О системе» показывает лишь версию MCU вида **`if2 - V2`**. Недокументированное решение: зайти в **Заводские настройки** и ввести пароль **`adbon`** — он разблокирует «Для разработчиков» / ADB. Дальше **Отладка по Wi-Fi** работает без USB-кабеля, а так как прошивка — сборка `userdebug`, **работает `adb root`**.

На реверс-инжиниринг ушёл целый вечер, поэтому оформлено отдельным гайдом для всех, у кого голова **XTRONS / Microntek / HCT / MTCE**:

📄 **[Включение ADB / «Для разработчиков» на китайских Android-головах (`adbon`)](docs/ENABLING-ADB-HCT-HEADUNITS.md)**

---

## Дизайн-система

Визуальный язык — выверенная «ночная» автомобильная тема: **аутентичный янтарь приборной подсветки BMW `#FF7E00`**, дозированно поверх почти-чёрного графита, матово (никакого глянца/стекла — блики в машине убивают всё).

**Токены цвета (ночь)**

| Токен | Значение | Назначение |
|---|---|---|
| `bgBase` | `#0A0B0D` | фон экрана |
| `surface` | `#15181B` | заливка плитки |
| `surfaceHi` | `#1E2226` | нажатая / фокусная заливка |
| `textPrimary` | `#C7CCD2` | основной текст (не чисто белый — блики) |
| `textSecondary` | `#8A9199` | вторичный текст |
| `accentAmber` | `#FF7E00` | единственный акцент |

- **Типографика:** **Inter** (400/500/600/700), табличные цифры. *(В дизайн-хендоффе был Archivo, но у Archivo нет кириллицы — интерфейс русский — поэтому взят Inter.)*
- **Взаимодействие:** каждое нажатие даёт «тройной сигнал» — уменьшение масштаба + янтарная рамка + подсветка фона — плюс вибро, потому что на экране в машине нажатие надо **видеть и чувствовать**.
- **Движение:** пружинный snap карусели, «дышащее» свечение, никакой лишней анимации.

Дизайн-референсы (рендеры макетов + полный спек токенов) — в [`docs/mockups/`](docs/mockups/).

---

## Архитектура

Чистый **Kotlin + Jetpack Compose**, без DI-фреймворка, компактно и читаемо. Логика, которую *можно* сделать чистой и покрыть юнит-тестами, *сделана* такой (геометрия, форматирование времени, маппинг воспроизведения, разрешение темы); Compose-UI проверяется скриншот-goldenами.

```
HomeActivity (единая Compose-Activity, объявлена как HOME)
 ├─ NavHost: home · music · apps · settings · bordcomputer
 ├─ SettingsStore (DataStore)         — сохранённые настройки
 ├─ AppLauncher / InstalledApps       — запуск + перечисление приложений
 ├─ RootShell / ShellCommands         — root-адаптивно (работает и без root)
 ├─ car/    IBusDecoder (чистый, юнит-тесты) · IBusReader (CP210x USB) ·
 │          IBusService (синглтон на весь процесс) · BordData
 ├─ diag/   AppLog · CrashHandler · AnrWatchdog · LogUploader
 ├─ update/ (UpdateChecker, ApkDownloader, ApkInstaller, RootDetector)
 └─ music/ (MediaSessionRepository, MusicController, MusicViewModel,
            MediaNotificationListener, PlaybackMapper, NowPlaying)
ui/
 ├─ home/   CarouselGeometry · HomeCarousel · TileCard · StatusRibbon ·
 │          RibbonClock · AmbientGlow · PageIndicator · MapBackground (MapLibre)
 ├─ bordcomputer/ BordComputerScreen
 ├─ apps/   AppsScreen
 ├─ settings/ SettingsScreen
 └─ theme/  Color · Theme · Type · Pressable · ScreenHeader
```

**Принципы**

- Тюнинг-константы (углы карусели, размеры, свечение) живут в чистых объектах — «характер» настраивается и раскатывается по OSA без правки вьюх.
- Всё деградирует мягко: без root и без сети.
- Лаунчер — это HOME-приложение: он **не имеет права падать**; все внешние интенты защищены.

---

## Структура проекта

```
app/                     Android-модуль (Kotlin/Compose)
  src/main/…             исходники + AndroidManifest + res (адаптивная иконка, шрифты Inter)
  src/test/…             JUnit + Robolectric юнит-тесты + Paparazzi скриншот-тесты
docs/mockups/            дизайн-референсы (рендеры экранов + спек токенов)
scripts/release.sh       сборка подписанного релиза + публикация OTA-манифеста
screenshots/             картинки для этого README
```

---

## Сборка и запуск

**Нужно:** JDK 17, Android SDK (compileSdk 34), Gradle (wrapper в комплекте).

```bash
export JAVA_HOME=/путь/к/jdk-17
export ANDROID_SDK_ROOT=/путь/к/android-sdk

# debug APK
./gradlew :app:assembleDebug

# юнит-тесты + проверка скриншотов
./gradlew :app:testDebugUnitTest :app:verifyPaparazziDebug
```

- `applicationId` = `online.k73.bmwlauncher` · `minSdk` 26 · `targetSdk` 33 · `compileSdk` 34.
- **Подпись релиза** читается из `keystore.properties` (в gitignore) — создайте свой keystore; из коробки собирается debug без подписи.

Установка на голову через ADB (или встроенным апдейтером):

```bash
adb install -r app/build/outputs/apk/debug/app-debug.apk
```

Затем назначьте приложением «Главный экран» по умолчанию (Настройки Android → Приложения по умолчанию → Приложение «Главный экран»).

---

## Тесты

- **Юнит-тесты** (JUnit4 + Robolectric) покрывают чистую логику: геометрию карусели, форматирование часов/даты, маппинг воспроизведения, разрешение темы, проверку обновлений и автомат автозапуска.
- **Скриншот-тесты** ([Paparazzi](https://github.com/cashapp/paparazzi)) рендерят каждый экран в 1280×720 головы и сравнивают с зафиксированными goldenами — визуальная регрессия валит CI. Эти же goldenы служат скриншотами в README.

```bash
./gradlew :app:recordPaparazziDebug   # перегенерировать goldenы
./gradlew :app:verifyPaparazziDebug   # проверить против goldenов
```

---

## Обновления по воздуху (OTA)

Приложение обновляет себя само, чтобы улучшения приезжали без флешки:

1. **Настройки → Обновление → «Проверить»** тянет JSON-манифест (`latest.json`) с описанием свежей сборки.
2. Если новее — кнопка становится **«Обновить»** и ставит её:
   - **с root** (если вдруг есть): тихо `pm install -r` + перезапуск,
   - **без root** (обычный случай): загрузка + интент системного установщика через `FileProvider`.

`scripts/release.sh` собирает подписанный релиз, заливает APK и пишет манифест. (Цель заливки специфична для окружения — направьте на свой хост.)

---

## История версий

| Версия | Главное |
|---|---|
| **1.6.38** | **Чёрный экран лечится сам** — детектор идёт по лестнице: убрать карту → пересоздать Activity → перезапустить процесс, и записывает, какой шаг вернул картинку; в отчёт добавлены хвост logcat, программная отрисовка того же окна (в обход GPU), счётчик кадров и число пересозданных GL-контекстов карты — одного случая теперь хватает, чтобы назвать сломанный слой |
| **1.6.32–1.6.37** | **Настоящие коды складывания зеркал** (0x39/0x3A — прежняя догадка открывала *окна*), складывание по зажиганию, а не по замку; экран-**пробник шины** для безопасной проверки телеграмм; плитка **ИВИ** рядом с YouTube |
| **1.6.31** | **Живучий детектор чёрного экрана** — считает пиксели собственного окна, поэтому отказ «окно пустое, главный поток жив» наконец оставляет улику даже без сети |
| **1.6.21–1.6.30** | Кнопка HOME всегда возвращает на карусель, «Назад» на карусели никуда не уводит; запись парктроника со счётчиками; **автоматика зеркал** (первая попытка); I-Bus **сам переподключается** после отвала USB; read-only **регистратор шины** с таймкодами кадров |
| **1.6.15–1.6.20** | Карусель плотнее и крупнее; **надёжная «Назад» на всех экранах** (+ физическая кнопка «Назад» — единственная реальная кнопка панели); **панельная «Музыка» → наш плеер** (редирект через AccessibilityService); **средняя скорость** в борткомпьютере (со сбросом); **запись парктроника** — опрашивает PDC на I-Bus, чтобы раскодировать его дистанции |
| **1.6.11–1.6.14** | Карта как **TextureView** (чинит чёрный главный экран после сна/пробуждения ACC); одним касанием **сброс залипшего лимита скорости ОБК** (гонг «>6 км/ч»); логирование нажатий железных кнопок; headless-старт Яндекса (без мелькания приложения) |
| **1.6.10** | Борткомпьютер логирует по одному примеру **каждого различимого типа сообщения I-Bus** — задел под топливо / расход / средние по поездке |
| **1.6.9** | **Живая наружная температура** в статус-ленте главного (заменяет эмблему X5); один общий ридер I-Bus на весь процесс |
| **1.6.6–1.6.8** | **Свой борткомпьютер** — читает BMW I-Bus через USB-адаптер CP210x (скорость · обороты · ОЖ · наружная температура); убран автозапуск i-Bus-аппа; тайлы карты проксируются через наш хост |
| **1.6.3–1.6.7** | **Живая карта-фон на главном** — MapLibre GL + OpenFreeMap, своя тёмно-янтарная стилизация (отказ от Yandex MapKit, чей проприетарный формат стиля так и не применился) |
| **1.6.0–1.6.2** | Эксперименты с аудиореактивным эквалайзером → **убраны** в пользу спокойной полосы перемотки (ROM блокирует захват аудио); устойчивые к перезагрузке ANR-отчёты; фиксы серого экрана |
| **1.5.7** | **Плитка YouTube** (открывается за per-app VPN с обходом DPI, настраивается на устройстве) |
| **1.5.6** | **Экран плеера v4** — обложка на весь фон + **живой янтарный эквалайзер** вместо полосы прогресса, новый ряд перемешать·пред·плей·след·лайк |
| **1.5.5** | Надёжный **холодный старт** Яндекса — авто-побудка при входе, ограниченные попытки, foreground-фолбэк |
| **1.5.0–1.5.4** | Встроенная диагностика (лог событий, захват crash/ANR, загрузка в один тап), полировка cold-start, фикс серого пустого экрана Музыки |
| **1.4.x** | **Редизайн Claude Design** — заполненные иконко-центричные плитки, обложка-конверт, рабочая навигация «назад», надёжное «сделать лаунчером по умолчанию», темнее палитра + типографика Inter, тройной отклик на нажатие |
| **1.3.x** | 3D-карусель iDrive на главном + тюнинг на устройстве (центровка под реальную ширину, размер, контраст) |
| **1.2.x** | Адаптивная иконка, настройка лаунчера по умолчанию, перемотка + лайк в Музыке, убрана вспышка i-Bus |
| **1.1.x** | Музыка «сейчас играет» (Яндекс `MediaSession`) |
| **1.0.x** | Скелет лаунчера, перезагрузка без root, OTA-самообновление, автозапуск |

---

## Планы

- **Борткомпьютер** — уровень топлива, расход и средняя скорость (у каждого — сброс), декодируются из I-Bus.
- Просмотр **плейлистов внутри лаунчера** (Яндекс `MediaBrowserService`, нужна проба на устройстве).
- Отражать **реальный статус «в избранном»** из медиа-сессии.
- Отдельная, светлее **дневная тема**.

*(Сделано с тех пор, как планы были написаны впервые: живая карта-фон, свой борткомпьютер на I-Bus, наружная температура в ленте, **средняя скорость поездки**.)*

---

## Полевые заметки — какие проблемы решили

Разработка лаунчера для китайской Qualcomm-головы вслепую, на реальной ежедневной машине, вскрывает проблемы, которых нет в документации. Главные:

- **Восстановление головы из вечного бутлупа.** Разовый эксперимент с root залил Magisk-патченный `boot_b`, который паникует на инженерном ядре → бесконечный цикл *логотип → чёрный экран*, из которого A/B-откат не выходил. Инструкции продавца (меню по кнопке Reset, USB-флешка) — для Rockchip, на Qualcomm не работают. Настоящее решение: зайти в **fastboot через 4-пиновый USB‑OTG** (см. ниже) и перезалить родной сток-boot (`fastboot flash boot_b …`). Данные, приложения и настройки уцелели (userdata общий у слотов A/B).
- **Какой USB-порт какой.** У этих голов три USB-канала: **6-пиновый = USB2 + USB3 (только хост**, для флешек) и **4-пиновый = USB_OTG** (сервисный). Только OTG определяется на ПК как **fastboot / EDL 9008** — любые попытки восстановления через хостовые порты не видят ничего. Reset+питание на OTG даёт *fastboot* (`Android Bootloader Interface`, 18D1:D00D), не EDL.
- **Кнопки панели нельзя перехватить — но можно перенаправить.** Живой захват `getevent` доказал: реальный код (`KEY_BACK`) шлёт только **«Назад»**; остальные кнопки MCU намертво запускает как штатные приложения Microntek, не отдавая перехватываемого события. Поэтому вместо перехвата — **AccessibilityService**, который ловит выход штатного приложения на передний план и открывает наш экран (панельная «Музыка» теперь открывает наш плеер).
- **Мелкие цели не срабатывают на этой LCD.** Маленькие «‹» у самого верхнего края часто не нажимались; увеличенные таргеты + аппаратный `BackHandler` на каждом экране решили и тач, и физическую «Назад».
- **Серые/чёрные экраны.** У Яндекс Музыки *несколько* `MediaSession` (пустая + играющая) — привязка к пустой рисовала пустой плеер; а GL-`SurfaceView` MapLibre теряет буфер после сна/пробуждения ACC → перешли на `TextureView`.
- **Фантомный гонг «>6 км/ч».** Удалённое штатное приложение залипило лимит скорости в кластере; одна запись в I-Bus (точная телеграмма, декомпилированная из того приложения) его сбрасывает.
- **Троттлинг YouTube.** Только YouTube идёт через always-on per-app `VpnService` (VLESS/Reality) — без root, остальное напрямую.
- **Парктроник пищит поздно по конструкции.** E53 молчит, пока не подъедешь близко. Дистанции есть на I-Bus (устройство `0x60`, отвечает по запросу) — сняв их, строим более ранний ступенчатый сигнал.

---

## Известные ограничения

- **Без root — намеренно.** То, что рутованный лаунчер мог бы делать (полностью тихие установки, принудительный HOME), делается вежливо, с подтверждением пользователя.
- LCD головы «поднимает» почти-чёрный к подсвеченному сине-серому — полностью тёмные экраны на фото выглядят синее, чем рендерятся.
- Интеграция с Яндекс Музыкой ограничена тем, что отдаёт её `MediaSession` (транспорт и «сейчас играет» гарантированы; просмотр библиотеки — нет).
- **Нет _постоянного_ root на устройстве.** В прошивке нет `su`/Magisk (и нет доступного USB для `fastboot`), поэтому ADB-root есть только при живом подключении — см. [гайд про `adbon`](docs/ENABLING-ADB-HCT-HEADUNITS.md). Поэтому VPN для YouTube сделан на постоянном `VpnService`, а не на root-туннеле.

---

## Стек

Kotlin · Jetpack Compose (Material 3) · Navigation-Compose · DataStore · `MediaSessionManager` / `NotificationListenerService` · **MapLibre GL** (тайлы OpenFreeMap) · **usb-serial-for-android** (CP210x / BMW I-Bus) · Coroutines · JUnit4 · Robolectric · Paparazzi · Gradle (Kotlin DSL) · адаптивные иконки · Inter (SIL OFL).

---

## Авторы и лицензия

Сделано **Roma** для своего E53 — реальной, ежедневно эксплуатируемой машины.

Разработка — **[Ostov ↗](https://ostov.dev/?utm_source=github&utm_medium=readme&utm_campaign=bmw-launcher)**

Опубликовано под **лицензией MIT** — см. [`LICENSE`](LICENSE).

> BMW, X5, E53 и iDrive — товарные знаки BMW AG. Это независимый некоммерческий любительский проект, **не аффилированный с BMW и не одобренный ею**. Иконка лаунчера и эмблема в статус-ленте — нейтральная оригинальная графика, а не роундель BMW.
